# ПЛАН сессии 2026-08-26 — Epic→Legendary оверкап + 3 прохода аудита

**Дата:** 2026-08-26 (СР, старт 12:49 MSK)
**HEAD на старте:** `31d679b` (synced с origin/main)
**Чекпоинт-компаньон:** `checkpoints/2026-08-26_epic_legendary_overcap_checkpoint.md`

---

## Общее направление работы за сессию

1. **Синхронизация и контекст** — push 5 незапушенных коммитов прошлой сессии
   (выполнено: `41201b7..31d679b` → origin/main), чтение чекпоинта
   `2026-08-27_generators_verification_checkpoint.md`, аудит текущего кода
   генераторов (прочитаны: EquipmentGenerator, LevelBoundaries,
   VerificationService, GeneratorTables, CheatPanel, GeneratorModule).

2. **Фича: Epic→Legendary генератор с шансом оверкапа** (ядро сессии).
   Проблема: сейчас `GradeToRarity(Transcendent) = Epic`, Legendary вообще
   никогда не генерируется; правила оверкапа в LevelBoundaries заданы, но
   ни один предмет их не использует. Задача пользователя: ввести процент
   шанса оверкапа (10–25%), чтобы НЕ каждая легендарка «улетала» на новый
   ранг по статам. Детальная логика — в чекпоинте, Phase B.

3. **Большая задача: минимум 3 ПОСЛЕДОВАТЕЛЬНЫХ прохода аудита** в основном
   потоке ИИ, с каждым проходом захватывая новые модули:
   - **Проход 1 — Архитектура** (Core: DI, interfaces, messaging/events,
     module system, Entry phases, Adapter-граница) → свой файл чекпоинта
     `2026-08-26_audit_pass1_architecture.md`.
   - **Проход 2 — Архитектура + модуль мира** (WorldGen/карта/тайлы/биомы,
     NPC-спавн) → `2026-08-26_audit_pass2_worldgen.md`.
   - **Проход 3 — Архитектура + мир + боевой контур** (Combat, Qi,
     Techniques, Formations, Body) → `2026-08-26_audit_pass3_combat_qi.md`.
   Каждый проход: чтение кода → фиксация проблем (critical/major/minor) →
   точечные исправления критичных → коммит. Аудит fresh: код сильно вырос
   после аудитов 08-21 (добавились Qi, техники, формации, культивация,
     тело, NPC-combat, торговля, генераторы, верификация).

4. **Верификация без локального ПК**: пользователь тестирует локально только
   после 19:00. До этого — headless-проверки
   (`GODOT_GEN_DEBUG=1` godot --headless) + `dotnet build`.
   Для вечернего теста пользователя — чит-кнопки (легендарка оружие/броня,
   батч ×20 со статистикой оверкапа).

5. **Документация**: обновить `docs/docs_v2/02_systems/LEVEL_BOUNDARIES.md`
   (шансы промоушена/оверкапа), `docs/docs_v2/07_ui/CHEAT_PANEL.md` (новые
   кнопки). Обновить `SESSION_CONTEXT.md` в конце сессии.

6. **Git**: коммит после каждой фазы, push в origin/main
   (токен настроен в remote).

---

## Ограничения и договорённости

- Без субагентов, без TodoWrite — вся работа в основном потоке (схема
  прошлых сессий).
- Раздельные файлы: план (этот) — направление; чекпоинт — детальные
  подэтапы + решения, обновляется после КАЖДОГО этапа.
- «Скрытого дискового хранилища» нет — персистентность обеспечивают:
  GitHub-репозиторий, файлы checkpoints/, SESSION_CONTEXT.md, worklog.md.
- Не трогаем: работающие headless-тесты (GODOT_NEWGAME, COMBAT_SIM,
  TRADE_DEBUG) — после изменений прогнать все.
