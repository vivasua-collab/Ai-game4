# Горячие клавиши (Hotkeys)

> **Назначение:** Движко-независимая спецификация горячих клавиш. Полный список действий, привязка к клавишам, контексты срабатывания, механика sticky-флагов.
>
> **Связанные документы:** `UI_DESIGN.md`, `06_player/INVENTORY_SYSTEM.md`, `01_architecture/DI_AND_EVENTBUS.md`, `05_data/SAVE_SYSTEM.md`.

---

## 1. Принципы

1. **Канонический набор:** F5, F9, Esc, E, B, R, X, F — обязательный минимум. Дополнительные клавиши (WASD, 1–9, мышь) — для управления игрой.
2. **3-слойная архитектура ввода:** Hardware → InputService (state) → ActionResolver (intent) → Module (execution).
3. **Sticky-флаги:** Одноразовые нажатия сохраняются в InputService как «sticky» до конца кадра; считываются и сбрасываются модулем.
4. **Действие зависит от контекста:** одна и та же клавиша может иметь разный эффект в зависимости от текущего UI-состояния (например, Esc закрывает окно если открыто, иначе — пауза).
5. **WASD отменяет click-to-move:** при активном клик-перемещении нажатие WASD сбрасывает цель.

---

## 2. Канонические горячие клавиши

### 2.1. Системные клавиши

| Клавиша | Действие | Контекст | Поведение |
|---------|----------|----------|-----------|
| **F5** | Быстрое сохранение | Игра (не в меню) | Вызывает `SaveRequestedEvent(QuickSave)`. Сохраняет текущее состояние в quicksave-слот. |
| **F9** | Быстрая загрузка | Игра (не в меню) | Вызывает `LoadRequestedEvent(QuickSave)`. Загружает состояние из quicksave-слота. |
| **Esc** | Пауза / Закрыть UI | Всегда | Если открыто окно — закрывает верхнее. Если окон нет — открывает Pause Menu. |

> **Автосохранение:** На v1 отключено. F5/F9 — ручное управление сохранениями. См. `05_data/SAVE_SYSTEM.md`.

### 2.2. Игровые клавиши

| Клавиша | Действие | Контекст | Поведение |
|---------|----------|----------|-----------|
| **E** | Взаимодействие | Игра (рядом с интерактивным объектом) | Подобрать предмет, открыть сундук, осмотреть труп, заговорить с NPC, активировать алтарь. |
| **B** | Инвентарь | Игра | Открыть/закрыть окно инвентаря. |
| **R** | Отдых / Медитация | Игра (не в бою) | Открыть окно отдыха: выбор длительности, режим (медитация/сон). |
| **X** | Снять экипировку / Специальное действие | Игра или Inventory | Контекстно: в Inventory — снимает предмет с выбранного слота; в игре — специальное действие (например, активация перка). |
| **F** | Сбор ресурса / Использование инструмента | Игра (рядом с harvestable) | Запускает добычу ресурса с текущим инструментом. Если инструмента нет — используется fallback «кулаки». |

### 2.3. Дополнительные игровые клавиши

| Клавиша | Действие | Контекст |
|---------|----------|----------|
| **W / ↑** | Движение вверх | Игра |
| **S / ↓** | Движение вниз | Игра |
| **A / ←** | Движение влево | Игра (спрайт зеркально отражается) |
| **D / →** | Движение вправо | Игра (спрайт зеркально отражается) |
| **Left Shift** | Бег (удержание) | Игра (×1.5 к скорости) |
| **Space** | Атака (базовая) | Игра (в радиусе врага) |
| **1–9** | Выбор слота хотбара | Игра (1=WeaponMain, 2=WeaponOff, 3–9=универсальные) |
| **J** | Журнал | Игра (открыть/закрыть журнал) |
| **T** | Техники | Игра (открыть/закрыть окно техник) |
| **C** | Лист персонажа | Игра |
| **Q** | Журнал квестов | Игра |
| **M** | Карта мира | Игра |
| **N** | Мини-карта | Игра (toggle видимости) |
| **` (Backquote) / F1** | Панель лога ввода | Всегда (диагностика) |

> **Канонический минимум** (из задачи): **F5, F9, Esc, E, B, R, X, F**. Остальные клавиши — рекомендуемое расширение.

---

## 3. Мышь

| Действие | Намерение | Контекст |
|----------|-----------|----------|
| **ЛКМ (LMB) короткий клик** | Движение к точке | Игра (без врага в точке) |
| **ЛКМ (LMB) на враге (ближний бой)** | Атака ближнего боя | Игра (в радиусе атаки) |
| **ЛКМ (LMB) на враге (дальний бой)** | Атака дальнего боя | Игра (weapon_main — ranged) |
| **ЛКМ (LMB) на предмете в UI** | Выбрать / перетащить | Inventory/Journal/любое окно |
| **ПКМ (RMB) короткий (< 300 мс)** | Трекинг / слежение | Игра (на враге/NPC) |
| **ПКМ (RMB) длинный (≥ 300 мс)** | Контекстное меню | Inventory (на предмете) / Игра (на объекте) |
| **Shift + ЛКМ** | Переместить в хранилище | Inventory (на предмете) |
| **Ctrl + ЛКМ** | Разделить стек | Inventory (на стаке) |
| **Колесо мыши** | Прокрутка списка / масштаб (карта) | UI / Map |

---

## 4. Контекстно-зависимые действия

### 4.1. Принцип

Одна клавиша может иметь разный эффект в зависимости от:
1. Текущего UI-состояния (HUD / Inventory / Journal / Pause / Dialogue).
2. Объекта под курсором / рядом с игроком.
3. Состояния игрока (в бою / в покое / в движении).

### 4.2. Примеры

| Клавиша | UI: HUD | UI: Inventory | UI: Dialogue |
|---------|---------|---------------|--------------|
| **Esc** | Открыть Pause Menu | Закрыть Inventory | Закрыть Dialogue |
| **E** | Взаимодействие с миром | (не используется) | Подтвердить реплику |
| **B** | Открыть Inventory | Закрыть Inventory | (не используется) |
| **Space** | Атака | (не используется) | Пропустить строку диалога |
| **1–9** | Выбор слота хотбара | (не используется) | Выбор реплики 1–9 |

---

## 5. Архитектура ввода (3 слоя)

### 5.1. Слой 1: Hardware → InputAdapter

Опрашивает keyboard + mouse на каждом кадре. Преобразует «сырой» ввод в структуру `InputFrameData`:

```
InputFrameData:
├── moveDirection: Vector2       # WASD/стрелки, нормализованная
├── isRun: bool                  # Shift удерживается
├── isLmbPressed: bool           # ЛКМ в этом кадре
├── isRmbPressed: bool           # ПКМ в этом кадре
├── rmbHoldDuration: float       # Длительность удержания ПКМ (для контекстного меню)
├── mouseWorldPos: Vector2       # Мировая позиция курсора (в промилле)
├── isOverUI: bool               # Курсор над UI
├── hotbarSlot: int?             # Выбранный слот 1–9 (null если не нажат)
├── stickyKeys: HashSet<KeyCode> # Одноразовые нажатия (E, B, R, X, F, F5, F9, Esc, J, T, C, Q, M, N)
└── frame: long                  # Номер кадра (для валидации sticky)
```

> `InputFrameData` — `readonly struct` (zero-GC). См. `01_architecture/PERFORMANCE_STRATEGY.md`.

### 5.2. Слой 2: InputService

Хранит состояние ввода, доступное модулям. Разделяет:
- **Удерживаемые состояния** (moveDirection, isRun, isLmbDown) — актуальны до конца кадра.
- **Sticky-флаги** (IsInteractPressed, IsInventoryPressed, IsRestPressed, ...) — одноразовые, считываются и сбрасываются в конце кадра методом `ResetFrameFlags()`.

### 5.3. Слой 3: ActionResolver → Module

Каждый модуль имеет свой `Tick()` и читает из InputService нужные ему флаги. Логика разрешения намерений:

```
PlayerModule.Tick():
  1. Если IsInventoryPressed → toggle UI state (Inventory)
  2. Если IsInteractPressed → InteractionService.TryInteract()
  3. Если IsRestPressed → открыть окно отдыха
  4. Если IsAttackPressed → CombatService.ExecuteAttack()
  5. WASD → PlayerService.ProcessMovement()
  6. HotbarSlot != null → HotbarService.SelectSlot()
  7. RMB long → ContextMenuUI.Show()
  8. ResetFrameFlags()  ← сброс sticky-флагов в конце кадра
```

---

## 6. Sticky-флаги

### 6.1. Принцип

Sticky-флаг — это одноразовый сигнал «клавиша была нажата в этом кадре». Устанавливается в InputAdapter, считывается в Module.Tick(), сбрасывается в `ResetFrameFlags()`.

### 6.2. Зачем

- **Предсказуемость:** модули гарантированно видят нажатие, даже если оно длилось <1 кадра.
- **Изоляция:** модули не опрашивают keyboard напрямую — только InputService.
- **Тестируемость:** в headless-тестах можно напрямую установить sticky-флаг и проверить реакцию модуля.

### 6.3. Список sticky-флагов

| Флаг | Клавиша | Кто обрабатывает |
|------|---------|------------------|
| IsInteractPressed | E | InteractionService |
| IsInventoryPressed | B | PlayerModule → UIService |
| IsRestPressed | R | RestService |
| IsSpecialActionPressed | X | ContextService |
| IsHarvestPressed | F | HarvestService |
| IsAttackPressed | Space / ЛКМ | CombatAdapter |
| IsDefendPressed | K (если есть) | CombatAdapter |
| IsMeditatePressed | M | MeditationService |
| IsSaveRequested | F5 | SaveModule |
| IsLoadRequested | F9 | SaveModule |
| IsEscapePressed | Esc | UIService (close/pause) |
| IsJournalPressed | J | UIService |
| IsTechniquesPressed | T | UIService |
| IsCharacterSheetPressed | C | UIService |
| IsQuestLogPressed | Q | UIService |
| IsMapPressed | M (alt: карта) | UIService |
| IsInputLogPressed | ` / F1 | InputLogPanel |
| HotbarSlotSelected | 1–9 | HotbarService |

---

## 7. Контекстное меню (RMB long)

### 7.1. Принцип

Долгое удержание правой кнопки мыши (≥ 300 мс) открывает контекстное меню для объекта под курсором.

### 7.2. Поведение

- **В Inventory (на предмете):** меню действий — Экипировать / Снять / Использовать / В духовное хранилище / В кольцо хранения / Разделить стек / Выбросить / Подробнее.
- **В мире (на NPC):** меню взаимодействий — Заговорить / Атаковать / Осмотреть / (в зависимости от отношения).
- **В мире (на объекте):** меню действий — Осмотреть / Взаимодействовать / (специфичные действия).

### 7.3. Реализация

InputAdapter измеряет длительность удержания RMB:
- Если длительность < 300 мс → `TrackingTargetEvent` (короткий клик = трекинг).
- Если длительность ≥ 300 мс → `ContextMenuRequestedEvent` (длинный клик = меню).

---

## 8. Хотбар (1–9)

### 8.1. Структура

9 слотов внизу экрана (HudLayer, see `UI_DESIGN.md` §6.1):

| Слот | Содержимое | Цвет рамки |
|------|-----------|------------|
| 1 | WeaponMain (основная рука) | Оранжевый |
| 2 | WeaponOff (вторичная рука) | Жёлтый |
| 3 | Универсальный (техника / расходник / инструмент) | Серый |
| 4 | Универсальный | Серый |
| 5 | Универсальный | Серый |
| 6 | Универсальный | Серый |
| 7 | Универсальный | Серый |
| 8 | Универсальный | Серый |
| 9 | Универсальный | Серый |

### 8.2. Привязка

- Слоты 1–2 — зеркалируют `EquipmentSlot.WeaponMain` и `WeaponOff`. При смене оружия в инвентаре — хотбар обновляется автоматически.
- Слоты 3–9 — игрок назначает вручную (drag&drop из инвентаря/журнала техник).

### 8.3. Активация

- Нажатие 1–9 → выбор слота.
- Если в слоте техника → `TechniqueSlotSelectedEvent` → CombatModule готовит технику.
- Если расходник → использовать.
- Если инструмент → взять в руку (для добычи).

---

## 9. Отключение ввода

### 9.1. Принцип

В определённых UI-состояниях игровой ввод отключается (например, открыто окно инвентаря → нельзя ходить).

### 9.2. Правила

| UI-состояние | Движение | Атака | Взаимодействие | Хотбар | Save/Load |
|--------------|----------|-------|----------------|--------|-----------|
| HUD (игра) | ✅ | ✅ | ✅ | ✅ | ✅ |
| Inventory | ❌ | ❌ | ❌ | ❌ | ✅ |
| Journal | ❌ | ❌ | ❌ | ❌ | ✅ |
| Pause Menu | ❌ | ❌ | ❌ | ❌ | ✅ |
| Dialogue | ❌ | ❌ | ✅ (только подтверждение) | ❌ | ✅ |
| Loading | ❌ | ❌ | ❌ | ❌ | ❌ |

### 9.3. Исключения

- **B (Inventory):** всегда работает, даже когда остальные клавиши заблокированы (кроме Loading).
- **Esc:** всегда работает (для выхода из любого UI).
- **F5/F9:** всегда работают (кроме Loading), чтобы можно было сохранить/загрузить из любого UI-состояния.

---

## 10. Цвета маркеров по редкости (для UI хотбара и слотов)

| Редкость | Цвет | RGB (0–1) |
|----------|------|-----------|
| Common | Серый | (0.7, 0.7, 0.7) |
| Uncommon | Зелёный | (0.3, 0.9, 0.3) |
| Rare | Синий | (0.2, 0.5, 1.0) |
| Epic | Фиолетовый | (0.7, 0.2, 1.0) |
| Legendary | Оранжевый | (1.0, 0.6, 0.1) |
| Mythic | Красный | (1.0, 0.15, 0.15) |

> Соответствует цветовой палитре темы «Древний Пергамент» (см. `UI_DESIGN.md` §2.2).

---

## 11. Известные ограничения и будущие расширения

| Ограничение | Статус | План |
|-------------|--------|------|
| Нет геймпада | 📋 Запланировано | Поддержка gamepad bindings в будущей итерации |
| Нет стамины бега | 📋 Запланировано | Shift-бег должен тратить выносливость |
| Нет клавиши сбора | ✅ Решено | F = сбор ресурса |
| Нет перестановки слотов хотбара | 📋 Запланировано | Drag&drop между слотами 3–9 |
| Нет кастомных биндов | 📋 Запланировано | Настройка клавиш в Settings |

---

## 12. Шпаргалка (для игрока)

| Категория | Клавиши |
|-----------|---------|
| **Сохранение** | F5 (save), F9 (load), Esc (pause) |
| **Перемещение** | WASD / стрелки, Shift (бег) |
| **Действия** | E (взаимодействие), F (сбор), Space (атака) |
| **Окна** | B (инвентарь), J (журнал), T (техники), C (персонаж), Q (квесты), M (карта), R (отдых) |
| **Хотбар** | 1–9 (выбор слота) |
| **Мышь** | ЛКМ (выбор/атака/движение), ПКМ короткий (трекинг), ПКМ длинный (контекстное меню) |
| **UI** | ЛКМ (drag&drop), Shift+ЛКМ (в хранилище), Ctrl+ЛКМ (разделить стек), Delete (выбросить) |
| **Диагностика** | ` / F1 (лог ввода) |

---

## 13. Связанные документы

- `UI_DESIGN.md` — UI Views, слои компоновки, тема.
- `06_player/INVENTORY_SYSTEM.md` — клавиши инвентаря (B, E, drag&drop).
- `01_architecture/DI_AND_EVENTBUS.md` — InputService, sticky-флаги, события.
- `05_data/SAVE_SYSTEM.md` — F5/F9, quicksave слоты.
