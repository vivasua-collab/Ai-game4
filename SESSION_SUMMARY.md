# Сводка сессий (обновляется при завершении каждой сессии)

Обновлено: 2026-09-22 (main Z.ai Code, сессии R32+R33; main = R33-коммит)

## Проект
Cultivation World Simulator (Ai-game4), Godot 4.7.2 .NET, C#
Репозиторий: https://github.com/vivasua-collab/Ai-game4

---

## Последние сессии (5 дней)

### 2026-09-22 R33 (пропущенная Фаза 4 аудита 09.22 — DI/lifecycle)
- Аудитор доставил отдельно отсутствовавшую в файле Фазу 4 (DI /
  Container / Startup / Lifecycle; зафиксирована по 949391c): P1-6 —
  **уже закрыт r32** (до доставки — реконструкция в 09_22_r32 была верна,
  страж F2 добавлен); P2-12/P2-13/P2-14 подтверждены и закрыты в R33.
- **P2-12 ResolveAll порядок:** итерация по _orderedRegistrations
  (контракт «registration order» теперь исполняется; прежде Container
  игнорировал ordered-параметр ctor-а) + согласование ordered↔словарь:
  мёртвые регистрации (prune-хвосты P1-6, self-bound ключ, украденный
  форвардом) не воскрешаются (иначе P1-6 вернулся бы через ResolveAll).
- **P2-13 startup контракт = FAIL-CLOSED:** все модули стартуют за одну
  попытку → AggregateException (полная диагностика); _initialized НЕ
  выставляется на провале — тик-луп заглушён; GameBoot: PushError по
  каждому провалу, глушит _PhysicsProcess, не входит в игру (в headless-QA
  проваленный бут = честный FAIL по таймауту, без VERDICT). Tick остаётся
  fail-open (изоляция сбоя в рантайме) — контракт задокументирован.
- **P2-14 cycle detection:** construction path (стек типов в текущей
  цепочке) — цикл ловится на ПЕРВОМ повторном входе с точным путём
  «A → B → C → A»; depth>50 остаётся страховкой.
- Сим №19 расширен секциями F/G/H (+10 проверок, стражи Фазы 4);
  живой бут с fail-closed — без провалов (ни один модуль не падает на
  Start). Ответы аудитору: `checkpoints/09_22_r33_ext_audit_phase4.md`
  (§2 — развилки P2-12/P2-13/P2-13-решения, §3 — статус «уже известных»);
  доки DI_AND_EVENTBUS §1.8; **регрессия 19/19 PASS**, build 0 err.

### 2026-09-22 R32 (внешний аудит 09_22_07_30 — P1-фиксы)
- **Верификация аудита** (5 фаз; Фаза 4 в файле ОТСУТСТВУЕТ — P1-6
  восстановлен анализом, зафиксировано в чекпоинте): все P1 проверены по
  коду + **runtime-верификация по запросу аудитора** (P1-5/P1-8) через
  новый QA-сим `Audit0922SimDebug` (GODOT_AUDIT0922_DEBUG, 16 проверок,
  префикс-прогон = FAIL с DEFECT-строками = runtime-подтверждение).
- **P1-5 EventBus ПОДТВЕРЖДЁН runtime** (исключение подписчика покидало
  Publish, прерывало fan-out, стэл re-entrant очередь роняла ЧУЖОЙ
  Publish) → фикс: пер-хендлерная изоляция + дрен в finally.
- **P1-8 traversal ПОДТВЕРЖДЁН** для Modules-layer SaveFileHandler
  (Adapter уже санировал — уточнение аудитору) → санация паритетом.
- **P1-6 DI override ПОДТВЕРЖДЁН** → фикс в 2 итерации: наивный prune ломал
  мульти-форвард-паттерн (Combat→TechniqueService, Corpse→NPCService,
  поймано регрессией 3 симов) → финал: pruning ТОЛЬКО при
  RegisterInstance-оверрайде; ключи Register<> независимы (стражи C3-C5).
- P1-1 TradeService : IWorldResettable (утечка стока+висячая сессия;
  NPC-ID=GUID — «npc_0» аудитора невозможен); P1-4 CorpseService :
  IWorldResettable (сброс работал транзитивно через NPCModule —
  функционально ОПРОВЕРГНУТ, закрыт контрактно; явный вызов удалён);
  P1-2 SaveAndQuit честный bool (сбой→тост+Playing); P1-7 DeleteSave
  честный bool (+P2-15 семантика); P1-3 NPC zero-GC (CopyStatesTo+
  персистентные буферы, вложенный обход — свой буфер).
- **P2 → бэклог 10 пунктов** (чекпоинт §4, указание аудитора не смешивать).
- Ответы аудитору + логи префикс/постфикс: `checkpoints/09_22_r32_external_
  audit.md`; QA: сим №19 AUDIT0922, **регрессия 19/19 PASS**, build 0 err.

### 2026-09-21 R31 (внешний аудит 20:30, Фазы 3–4 + план аудита доков)
- **Верификация `upload/audit_09_21_20_30.txt`** (фазы писались по
  cb7b565 — ДО r30-1, поэтому Ф4 частично была уже закрыта; всё
  перепроверено по текущему HEAD):
  - **Фаза 3 — 5/5 тезисов ПОДТВЕРЖДЕНЫ и закрыты** (b5512bc):
    Ф3-1 LOS-звери (CombatRangeGateService + IAnimalService — стрела
    больше не летит сквозь дерево по волку); Ф3-2 **AoE в пустую
    область** (3 гейта кастера для RangedAoe валидируют ПРИЦЕЛ:
    границы карты + радиус; AttackIntentEvent с пустой целью —
    контракт это допускает; CombatService пропускает пустоцельный
    залп «Ци срывается волной в пустоту»; не-AoE без цели — Rejected);
    Ф3-3 хоуминг на мёртвую цель (AoEResolver.TryResolveAliveTile —
    seek только к живым: NPC IsAlive / AnimalInfo.IsAlive); Ф3-4 спавн
    болта из ЦЕНТРА тайла (TileCenterOffsetMilli=500 — единая
    семантика с рендерами, пол-тайла рассинхрона исчезли); Ф3-5 стрелы
    по зверям (ProjectileRenderer + IAnimalService — полёт+вспышка).
  - **Фаза 4 lifecycle — 6/11 уже закрыты r30-1 (B1, перепроверено);
    5 закрыто в R31**: PerkService (IWorldResettable), TargetingService,
    PlayerCombatAdapter, DialogueService (reuse EndDialogue),
    CombatService-хвост (_lastAttackPotency/IsRanged). Главный кейс —
    **NPCRelationshipService: IWorldResettable + ISaveable** (новый
    блок `"npc_relationships"`, RestoreOrder после npc; ДВА дефекта
    закрыты: отношения переживали NewGame + после Load падали в
    Neutral — сейв хранил AttitudeScore в мёртвое поле). 1 тезис
    (RestoreState→void integrity gap) ПОДТВЕРЖДЁН как архитектурное
    ограничение и ОТЛОЖЕН — долг в плане аудита доков.
  - QA: COMBAT_SIM 3h(d) НОВЫЙ (пустоцельный залп/no-aim/non-AoE
    Rejected), SaveLoadSim 1m (обида −15 → мутация +30 → Load → −15),
  round-trip 21 блок; регрессия **18/18 PASS**; build 0 err; push OK.
- **Модуль 17 «торговля» (вопрос пользователя):** Trade — реальный
  отдельный модуль (ровно 17 папок в Modules), № присвоен КОЛРЕКТНО,
  УЖЕ документирован (MODULE_STRUCTURE «17 модулей» + §2.17 +
  TRADE_SYSTEM.md, аудит 09_06). Хвосты: README:25 «16 модулей»
  (устарело), §2.17 не знает про экономику внутри Trade (R22-1 стабы
  CurrencyExchangeService живут там). → всё в план аудита доков.
- **`checkpoints/plans/2026-09-21_r31_doc_audit_plan.md` (НОВЫЙ, ПЛАН —
  обновления НЕ применены по указанию пользователя):** реестр дрейфа
  docs_v2 vs код (r30-1 фиксил 10 пунктов БЕЗ синка доков; R31
  намеренно), 5 проходов (P0 «доки врут»: SAVE_SYSTEM 19→21 блоков +
  «трупы вне сейва» — ложь с r30-1; README 16→17; §2.17+экономика),
  grep-критерии приёмки, 3 открытых вопроса (RestoreState-void,
  legacy AttitudeScore, экономика как 18-й модуль?).

### 2026-09-21 R30 (аудит 09_21 + плейтест П1–П7)
- r30-1 (98fdc28): внешний аудит — 10/11 подтверждены и закрыты
  (A2 пре-валидация+рефанд, A3 глобальные координаты мыши AoE, A4
  гейт реализуемости предметов, A5 CorpseService ISaveable — трупы+
  full-loot в сейве, A6 камни NotUsable, B1 IWorldResettable ×6,
  B2/B3 зарядник-инвариант stones−N⇔buffer+N, B4 мёртвая NPC-цель,
  B5 спуск ауры по techniqueId, B6 дабл-стек баффов), A1 опровергнут
  (+харденинг explicit potency). Доков НЕ касался (задолженность).
- r30-2 (09289fa): плейтест П1–П7: контраст формаций на песке (тёмная
  подложка), радиус зарядки формации (пауза дрейна Ци вне радиуса,
  резюм), FormationDescriptions (тост+подзаголовок действия),
  K-окно модальное+пауза+тумблер PauseOnModalWindows, медитация
  спец-слот «☯» вне 1–9, F3 FPS-счётчик, guard триангуляции полигона.

### 2026-09-20/21 R23–R29 (мультибой → VFX)
- R24 мультибой-C (реестр входов, UI-сессия только для пары с игроком);
  R25 AoE-A залп (int-геометрия форм, месть нейтралов бесплатно);
  R26 индикация заряда в хотбаре; R27 TargetingService (Tab-цикл);
  R28 хоуминг-A (Reynolds seek + turn-budget, снаряды-сущности);
  R29 VFX (AoeFxRenderer, HomingProjectileRenderer, ElementPalette,
  аура удержания; QA 3k + скриншоты opengl3+VLM).

### 2026-09-20 R22 (экономика-стабы + аудит боя)
- Канон: **1 камень = 100 золота; смертным не владеть камнями**
  (EconomyConstants + ICurrencyExchangeService — стабы, живут в
  Trade-модуле). Внутренний аудит боя: 21 дефект (CMB-1/2/3 — закрыты
  R23/R24). Доки ECONOMY_SYSTEM.md.

### 2026-09-19/20 R18–R21
- HP-бары/F7; ПКМ-расходники; R20 8 багов плейтеста; R21 readiness-
  модель (ходы удалены), CLASH-парирование, броня §5.2, авто-RawQi.

---

## Текущее состояние игры

### Что работает
- Мир 500×500 (полигон 50×50); harvest/инвентарь/кукла/пояс/ПКМ/торговля;
  **Save/Load 21 блок** (R31: +npc_relationships; r30-1: +corpses
  full-loot); Qi/техники/формации/зарядник/медитация (спец-слот ☯)
- **Бой:** readiness-модель, мультибой-C (толпы дерутся без слота),
  AoE-формы (конус/круг/полукруг/линия, **каст в пустую область** R31),
  хоуминг-снаряды (центр тайла, seek только к живым R31), Tab-таргетинг,
  LOS для NPC∪звери (R31), clash, месть нейтралов; VFX-слой R29;
  K-окно с паузой + тумблер; F3-FPS
- **Lifecycle:** IWorldResettable 30+ сервисов (R32: +Trade, Corpse
  контрактно); NewGame/LoadGame чистые (QA REASSEMBLY)
- **Инфраструктура (R32):** EventBus exception-изоляция + честный
  re-entrancy-дрен; честные Save/DeleteSave bool; санация имён слотов;
  DI override prune (RegisterInstance-only); NPC hot path zero-GC
- 19 QA-симов PASS (~3.5 мин полный прогон; +AUDIT0922 R32)

### Что НЕ работает / очередь
- ❌ **P2-бэклог аудита 09.22** (чекпоинт 09_22_r32 §4, 10 пунктов:
  RestoreState→bool, legacy AttitudeScore, ConfigureAwait-гард,
  Trade-декомпозиция, Corpse FNV-сид, Initialize-идемпотентность,
  GetAllSaves-мета, сейв-версии, Load-after-Reset)
- ❌ **План аудита доков R31 — ОБРАБОТАТЬ** (checkpoints/plans/
  2026-09-21_r31_doc_audit_plan.md: 5 проходов, обновления НЕ применены;
  +новые дрейф-пункты от R32-фиксов — в следующий проход плана)
- ❌ R30+ отложено пользователем: мультибой-B, живые зоны AoE-B,
  SpareAllies-ауры (ждут party-систему)
- ❌ Архитектурный долг: legacy AttitudeScore в NPCSaveEntry; золотой
  кошелёк/проводка экономики (стабы готовы); R20-№5 книга техник UI;
  NPC-урон тюнинг

---

## Замороженные решения (НЕ нарушать)
- Godot 4.7.2 .NET; Qi = long; Permil integer math (ЗАПРЕТ 3.9)
- **Курс 1 камень = 100 золота; смертным НЕ владеть камнями** (R22-1)
- NPC умирают только в бою/от проклятий/ядов (no old_age)
- Документация первична: docs_v2 ≠ код = баг → синхронизировать
  (сейчас применён ОСОБЫЙ РЕЖИМ: план синка создан, применение
  отложено пользователем до многократной обработки плана)
- НЕ запускать Next.js DEV сервер

---

## Следующие шаги
1. Ответ аудитора на чекпоинты 09_22_r32 + **09_22_r33 (Фаза 4
   обработана: P1-6 уже в r32, P2-12/P2-13/P2-14 закрыты R33)** — его
   следующая фаза: матрица state→reset→save→restore→dispose→tick по всем
   17 модулям
2. P2-бэклог аудита 09.22 — приоритизация пользователем
3. Обработка плана аудита доков (Pass 1 → 5; + дрейф-пункты R32)
4. R30+ (после снятия моратория): мультибой-B / AoE-B зоны / ауры

---

## Предупреждения
- Субагенты — ТОЛЬКО последовательно, ≤1 одновременно (START_PROMPT
  §14.2); экономить токены — flash/haiku где возможно
- Эпизод ≤45 мин → коммит+пуш+чекпоинт; метка NEXT: в worklog
- QA: `bash tools/qa_regression.sh` (19 симов ~3.5 мин); единичные —
  GODOT_NEWGAME=1 GODOT_XXX=1 …/godot_bin… --headless; вердикт grep
  'VERDICT'; тяжёлые прогоны — в конце эпизода
- Среда сбрасывается (dotnet/godot dentry-ghost): `bash cold_start.sh`
- Токен: /home/z/my-project/.auth/github.token (не запрашивать, если есть)
- Push работает (R31 на GitHub до R32)
